spec:
  name: docker-stellar-core-horizon
  services:
  - dockerfile_path: Dockerfile
    git:
      branch: master
      repo_clone_url: https://github.com/fxmalang/quickstart.git
    name: docker-stellar-core-horizon
    run_command: /start --pubnet
